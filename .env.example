# PostgreSQL container bootstrap settings.
POSTGRES_USER=golek
POSTGRES_PASSWORD=golekpass
POSTGRES_DB=golekthreat

# Backend database URL used by the API container.
DATABASE_URL=postgresql+psycopg://golek:golekpass@postgres:5432/golekthreat

# Published host ports.
API_PORT=8000
FRONTEND_PORT=3000
